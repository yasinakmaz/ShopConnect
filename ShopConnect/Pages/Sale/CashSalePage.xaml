<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShopConnect.Pages.Sale.CashSalePage"
             Title="CashSalePage">

    <Grid x:Name="GrdMain">
        <Grid x:Name="GrdFill" RowDefinitions=".20*,.70*,.10*">
            <Grid x:Name="GrdTop" Grid.Row="0" Margin="10">
                <VerticalStackLayout>
                    <Grid ColumnSpacing="10" ColumnDefinitions=".20*,*,.20*">
                        <SearchBar Grid.Column="0" Placeholder="Seçim"></SearchBar>
                        <Entry Grid.Column="1" Placeholder="Unvan"></Entry>
                    </Grid>
                    
                    <HorizontalStackLayout>
                        
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Grid>

            <Grid x:Name="GrdCenter" Grid.Row="1" BackgroundColor="NavajoWhite">
                
            </Grid>

            <Grid x:Name="GrdBot" Grid.Row="2">
                
            </Grid>
        </Grid>
    </Grid>
</ContentPage>